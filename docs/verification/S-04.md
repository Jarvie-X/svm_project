# S-04 verification

Date: 2026-09-04

Commands run:

```text
python -m pytest -q
python -m wine_prediction --values 13.2 1.78 2.14 11.2 100 2.65 2.76 0.26 1.28 4.38 1.05 3.4 1050
python -m wine_prediction --values 1 2
```

Results:

- Automated tests: **20 passed**.
- Guidance exposes all thirteen fields in model order.
- The complete known Wine Recognition sample was accepted and returned
  **Class 0**.
- Incomplete entries (0, 12, and 14 values, plus an empty mapping) were
  rejected with field/count correction messages.
- Representative invalid entries (blank, non-numeric, NaN, infinity, zero,
  and negative) were rejected with field-specific correction messages.
- The scripted CLI flow displays the guided field list and completes one
  prediction without interaction.
